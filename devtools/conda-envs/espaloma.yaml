name: test
channels:
  - conda-forge
  - defaults
  - salilab
  - omnia
dependencies:
  # Base dependencies
  - python
  - pip
  # 3rd party
  - modeller
  - rdkit
  - numpy
  - matplotlib
  - scipy
  - openforcefield
  - openmmforcefields
  # Testing
  - pytest
  - pytest-cov
  - codecov
  - nose
  - nose-timer
  - coverage
  - qcportal
  # Pip-only installs
  - pip:
      - torch
      - dgl
